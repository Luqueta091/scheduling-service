# Migrations

Coloque aqui os scripts de migração (SQL ou TypeScript). Use o padrão incremental `YYYYMMDDHHMM__descricao.sql`.

O runner atual apenas registra que não há migrações pendentes. Em tarefas futuras, substitua por uma execução real (por exemplo, usando `node-postgres-migrate`).
